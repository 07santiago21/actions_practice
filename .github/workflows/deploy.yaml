name: "deploy to server"

on:
  push:
    branches: ["main"]
  
jobs: 

  first_job:
    runs-on: ubuntu-latest
    steps:
    
      - name: hola munndo
        run: echo "hola mundo"

  second_job:

    needs: first_job
    runs-on: ubuntu-latest
    
    steps:
    
      - name: hola munndo 2
        run: echo "hola mundo 2"
